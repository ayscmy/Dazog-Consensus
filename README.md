# 大纵观共识 · 价格行为学多周期盯盘

一个零依赖的本地盯盘工具，盯 A股（股票/指数/ETF）+ 国内期货（商品期货 + 股指期货 + 国债期货）。
数据来自**新浪财经实时行情**（服务端代理，约 3 秒级延迟，非逐笔）。

## 运行

**方式一（推荐，双击）**：直接双击 `start.bat`，会自动启动并用浏览器打开看板。

**方式二（命令行）**：
```bash
node server.js
```
然后浏览器访问 http://localhost:8080

> 需要 Node.js（已用本机受管 Node 22）。若你电脑已装 Node，也可直接 `node server.js`。

## 部署到服务器（上传即用）

- 全部前端 API 均用**相对路径**（`/api/...`），无 localhost 硬编码；`/robots.txt` 与 `/sitemap.xml` 按请求 Host 动态生成，**换任何域名/端口都自动适配**；JSON-LD 与 canonical 在页面加载时自动注入当前地址。
- 方式一（推荐）：服务器装 Node.js，把 `pc-dingpan/` 整个目录传上去，`node server.js` 即可，端口用环境变量 `PORT` 指定（如 `PORT=80 node server.js`）。
- 方式二（宝塔/Nginx 反代）：Node 跑在内部端口（如 8080），Nginx 配置 `proxy_pass http://127.0.0.1:8080` + `proxy_set_header Host $host`，即可域名访问并支持手机/平板。
- 数据文件说明：`futures.json`（主力合约，换月后重跑 `gen_futures.py` 校准）、`stocks.json`、`macro_guide.json`（教材框架）、`spot_history.json`（现货历史，随录入增长，部署时一并上传）。
- 浏览器兼容：桌面/手机/平板均可访问（含响应式布局），建议 Chrome/Edge/Safari 现代浏览器。

## 页面结构（无左右结构，全屏视图）

### 主页：左商品列表（约4/5） + 右自选栏（约1/5）
- 进入即见**全量商品列表**（期货 128 + 精选股票 180 = 308；**「全部股票」覆盖全市场 5547 只 A 股**，默认加载前 100 只 + 加载更多 + 搜索全市场/指定代码必加载），分类 Tabs：**全部股票 / 精选股票 / 所有期货 / 精选期货 / 自选股票 / 自选期货**
- **右侧自选栏（1/5 宽）**：顶部「自选股票 / 自选期货」切换；点击列表名称前的 ☆ 加入自选、★ 取消自选；自选栏内点 ✕ 移除，点行进分析页
- **宏观信息时刻展示**：顶部常驻实时宏观条 + **📅 每日宏观速览** + **🗓 每周宏观速览**（真实数据聚合，每日=当日大盘/商品/汇率综合判断，每周=日线近5日累计涨跌+周线偏多偏空）
- **📚 宏观策略手册（教材《证券宏观分析与投资策略》框架，可折叠）**：① **宏观分析五维度**仪表盘（经济基本面/货币流动性/财政政策/产业监管/外部宏观，每维=教材观测指标+传导逻辑+当前真实代理数据）；② **中国重要会议日历**（全年21场会议时间轴，当前月琥珀高亮、下一场蓝框+倒计时提示，含政策传导总路径与三层会议看宏观方法）；③ **业绩披露季**（年报/一季报/半年报/三季报时间表+当前所处阶段+业绩期操作纪律）；④ **产业周期五阶段**（导入/成长前/成长后/成熟/衰退：收入利润/估值参与者/赔率）；⑤ **两会目标跟踪表**（GDP/就业/CPI/专项债等10项）；⑥ **套保原则与阈值**（简单版击穿20天线/BBI、复杂版多因子）；⑦ 教材配置建议。数据源：`macro_guide.json`（教材结构化），实时数据由 `/api/macro` 动态附加
- **股票详情增强**：📊 **基本面**卡（PE/PB/总市值/换手/52周高低 + 当前价 vs 5/20/60日均线对比 + 60日区间）；💧 **资金流**卡（主力/超大/大/中/小单净流入，近8日历史，东财源）；📰 **资讯**卡（东财按品种名搜索）
- **期货详情增强**：🏷 **现货价格**卡（记录式系统：自动抓伦敦金银 + 手动录入，**录入字段=价格/日期(默认今天)/数据来源下拉(上海有色·生意社·交易所·新闻媒体·海关部委)/备注**，历史保存本地 spot_history.json，自动与上次对比）；💧 **资金流（持仓量变化）**卡（期货无资金流，以持仓量增减近似资金进出）；📇 **相关合约**卡（同品种主连/主力/其他月份，点击切换分析）；🔗 **期权链**入口（实时数据需付费源，如实标注）；📰 **资讯**卡
- **资金流展示**（股票）：💧 今日主力净流入**大号高亮卡**（▲红流入/▼绿流出 + 超大/大/中/小单标签）+ **近8日主力净流入柱状图**（红流入绿流出）+ 5日累计 + 明细表；期货为持仓量变化柱状图
- **SEO/GEO 优化**：head 完整 meta（title/description/keywords **核心+长尾共 90+ 词**）、Open Graph、Twitter Card、JSON-LD 结构化数据（WebApplication+FeatureList）；body 隐藏 GEO 长尾关键词区（覆盖品种/指标/策略/宏观/业绩/风控等）；`/robots.txt` + `/sitemap.xml` 路由
- **每周关注板块 + 每日关注板块**（真实行情按板块聚合，点击板块跳转对应分类并搜索）
- 列表行显示：☆自选/名称/代码/分类标签、最新价、涨跌幅、1h 方向、距 S/R、时间；**涨红跌绿**、接近 S/R 行琥珀高亮
- 数据时刻自动刷新（1/2/3/5/10s 可选）

### 分析页：独立全屏页面（点击任意品种进入）
- **时间周期全部排列**：1分钟 / 5分钟 / 15分钟 / 30分钟 / 1小时 / 4小时 / 日线 / 周线 / 月线 共 **9 张 K 线图**，焦点品种显示所有时间周期，数据时刻自动刷新（最后一根随实时价跳动）
- **4 小时定阶段（P0）**：最高级别方向过滤，顶栏 4h 徽章 + 与 1h 方向一致性提示（同向顺势 / **背离警示逆势段**）
- **多周期分析放在时间周期后面**：1h 定方向 / 30m 看段落（CHoCH 转势）/ 5m 找入场（Pin·吞没·内包·SFP）、S/R 支撑压力位、共振预警
- **决策链指标共振（P0，对齐用户 5 层决策链）**：「📊 指标共振」卡显示 **4h/1h MACD（12/26/9）**、**30m KD（9/3/3，K/D/J + 超买超卖）**、**5m MA（MA5/MA20 多头/空头排列）**；**🚫 铁律自动执行**——30m J 值超买(>80)时 5m 严禁开多仓、超卖(<20)时严禁开空仓，直接拦截决策卡（blocked+拦截原因），未拦截时也以铁律提示展示
- **当日分时（P0-2）**：「🕐 当日分时」卡显示价格线+均价线+量能柱，随行情自动刷新（股票走东财 trends2、期货走新浪 getMinLine）
- **缠论基础版（P1-1）**：「🧭 缠论+经典形态」卡：1h 级别自动划分分型/笔/中枢区间/一类买卖点
- **经典形态识别（P1-2）**：双底(W)/双顶(M)/头肩底/头肩顶/楔形/三角 自动识别（1h+4h 级别），标注看多看空
- **斐波那契 + 趋势线（P2 体验）**：4h 图自动叠加斐波那契回撤位（0.236~0.786）与上升/下降趋势线；日线图底部小地图导航
- **全列表信号扫描**：主页「📡 全列表扫描」按钮按当前分类批量扫描（4h阶段+1h方向+共振+信号+缠论买卖点+形态综合评分），点击查看详情
- **决策卡（P0）**：每个品种给出方向倾向（做多/做空/观望）、依据、**入场区间、止损、目标（1:2 盈亏比+反向S/R）、盈亏比、单笔风险金额**（按账户资金×风险比，可调）；铁律触发时显示「🚫 观望（铁律拦截）」
- **持仓管理（P0）**：💼 按钮添加持仓（代码/方向/手数/开仓价/止损/止盈/乘数），列表行显示 ▲多/▼空；分析页显示实时浮盈亏（点数+金额）；**到止损/止盈自动弹窗+发声**
- **关联商品**：同板块/同品种的关联标的，点击切换进入其实时 K 线
- 顶部常驻宏观条 + 返回列表按钮 + 加自选按钮
- 到支撑/压力位时**顶部弹窗**显示品种、价位、**什么形态**，带「＋自选」+ 报警音；共振时红/绿高亮

## 功能清单

- 实时行情表格（分批并发刷新，支持 308 个大列表不卡）；**涨=红、跌=绿**
- **一键加自选**：输入任意代码（`600519`、`ag0`、`ag2609`）实时校验后加入；自选股票 / 自选期货独立分类
- 支撑压力雷达（默认开启）：滚动扫描全市场 S/R 临近，弹窗醒目提醒
- **每日推荐方案窗口**（📋 按钮，**三 Tab 切换**）：①🕗 **竞价股票**（今开 vs 昨收跳空+竞价强度）②🌆 **尾盘股票**（当日表现+多周期技术）③📊 **期货分析**（4h+1h+30m+5m 多周期+共振）；每项附**操作理由**（宏观+技术），点击任一行查看详情
- 股票专属：早盘竞价（真实今开/昨收跳空+强度）、尾盘分析（实时价+区间分位+真实宏观）
- 期货专属：关联品种历史走势曲线（现货需付费源，以真实关联合约替代并标注）
- 每个分析区块下方附「🤖 AI 自动分析：仅供参考，非投资建议」
- 浅色 / 深色主题切换

## 技术要点

- **时间周期数据源**（全部真实、新浪一家）：
  - 股票：1m 走 `quotes.sina.cn`；5/15/30/60m 走 `money.finance.sina.com.cn`；日线用 `scale=240`（新浪返回日线）；**4h 由 60m 聚合**；周/月由日线聚合
  - 期货：1/5/15/30/60/240m 走 `getFewMinLine`（type=240 原生 4 小时线）；日线走 `getDailyKLine`；周/月由日线聚合
- 新浪强制 Referer，需服务端代理；价格直接 `parseFloat`，无缩放问题（东财/腾讯分钟线在本环境不可用，已弃用）
- 期货涨跌以**昨结算价**为基准；股指/国债期货（IF/IC/IH/T/TF/TS/TL）独立列解析
- S/R 阈值为距价位百分比（默认 0.4%）；反转信号基于已收盘 K 线

> 服务端接口：`/api/markets`、`/api/quotes`、`/api/kline`（1m..month）、`/api/alltf`（9帧）、`/api/analyze`（多周期+决策卡+指标+缠论+形态+fib）、`/api/minute`（分时）、`/api/sr-scan`、`/api/batch-scan`（全列表信号扫描）、`/api/related`、`/api/macro`（宏观+手册）、`/api/focus-sectors`、`/api/recommend`（每日推荐三分组）、`/api/fundamental`（股票基本面+历史对比）、`/api/fflow`（资金流/持仓量）、`/api/news`（资讯）、`/api/spot` + `/api/spot-record`（现货记录系统，`spot_history.json` 持久化）。另提供 `/robots.txt` 与 `/sitemap.xml`（SEO）。
> 期货全量清单由 `gen_futures.py` 校准生成 `futures.json`（主连+主力，换月后可重跑）；股票清单由 `gen_stocks.py` 生成 `stocks.json`。

## 说明

- 行情与 K 线均来自新浪财经（服务端代理，约 3 秒级延迟，非逐笔）。K 线取主连/具体合约，跨合约换月可能有跳空。
- 期货主力合约代码在 `futures.json` 中，换月后可重跑 `gen_futures.py` 自动校准。
- 仅作盯盘参考与学习，不构成任何投资建议。
